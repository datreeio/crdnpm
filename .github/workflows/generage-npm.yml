name: Generate NPM package and push to NPM

on: [push]

jobs:
  Create-npm-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: datreeio/CRDs-catalog
          branch: main
        run: |
          ls -la
      # - name: Generate NPM package
      #   uses: actions/generate-npm-package@v1
      #   with:
      #     package-name: ${GITHUB_REPOSITORY}
      #     package-version: ${GITHUB_SHA}
      #     package-description: ${GITHUB_REPOSITORY}
      #     package-author: ${GITHUB_ACTOR}
      #     package-license: MIT
      #     package-private: false
      #     package-scripts:
      #       start: npm start
      #       build: npm run build
      #       test: npm run test
      #       lint: npm run lint
      #       prepublish: npm run prepublish
      #       publish: npm publish







      # - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      # - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      # - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # - name: Check out repository code
      #   uses: actions/checkout@v3
      # - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      # - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}
      # - run: echo "🍏 This job's status is ${{ job.status }}."